/** @type {import('next').NextConfig} */

// import withBundleAnalyzer from "@next/bundle-analyzer";

const nextConfig = {
  reactStrictMode: false,
};

// const a = withBundleAnalyzer({
//   enabled: true,
//   openAnalyzer: true,
// });

// export default a(nextConfig);
export default nextConfig;
